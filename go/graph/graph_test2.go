package graph

/*
func _TestBFS(t *testing.T) {
	adjList := GenerateGraphInAdjacencyList(7, 40, true, true, 20)
	bfsOrder := BFS(adjList, 0)

	adjList.Print()
	for i := bfsOrder.Front(); i != nil; i = i.Next() {
		fmt.Print(i.Value, " ")
	}

	fmt.Print("\n")
}

func _TestDFS(t *testing.T) {
	adjList := GenerateGraphInAdjacencyList(5, 40, true, true, 20)
	dfsOrder := DFS(adjList, 0)

	adjList.Print()
	for i := dfsOrder.Front(); i != nil; i = i.Next() {
		fmt.Print(i.Value, " ")
	}

	fmt.Print("\n")
}
*/
